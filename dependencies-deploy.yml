name: mercs 
channels:
  - defaults
  - conda-forge
  - pytorch
dependencies:
  - python>=3.6
  - pip
  - ipython
  - pip:
      - numpy
      - scipy
      - pandas
      - networkx
      - scikit-learn
      - dask
      - toolz
      - tornado
      - pydot
      - shap
      - decision-tree-morfist
