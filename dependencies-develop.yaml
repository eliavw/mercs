name: eliatest
channels:
    - defaults
    - conda-forge
    - pytorch
dependencies:
    # Defaults
    - pytest
    - jupyterlab
    - mkdocs
    - mkdocs-material
    - pygments
    - pylint
    - xgboost
    - pip:
        - mkdocs-minify-plugin>=0.2
        - pymdown-extensions
        - semantic_version
        - matplotlib
